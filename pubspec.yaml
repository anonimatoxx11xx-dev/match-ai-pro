name: match_ai_pro
description: Match AI Pro - real football data and AI analysis
publish_to: "none"
version: 1.1.0+2
environment:
  sdk: ">=3.7.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - data/latest.json
